% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm_unzip.r
\name{nm_unzip}
\alias{nm_unzip}
\title{Unzip a NONMEM File}
\usage{
nm_unzip(
  x,
  rundir = normalizePath(getOption("nmDir", getwd())),
  path = file.path(rundir, x),
  extension = ".xml",
  filename = paste0(x, fxs(extension, ".   ")),
  zipped = file.path(path, filename),
  tmpdir = tempdir(),
  temp = FALSE,
  outdir = ifelse(temp, tmpdir, dirname(zipped)),
  call = getOption("unzip.call"),
  quiet = TRUE,
  ...
)
}
\arguments{
\item{x}{character (run name)}

\item{path}{path to directory enclosing the NONMEM run directory}

\item{extension}{character: extension to append to run}

\item{filename}{character: supersedes run, extension}

\item{zipped}{character: full path to zipped file (7z extension is optional); supersedes other arguments if supplied}

\item{tmpdir}{character: path to temporary working directory if \code{temp} is true}

\item{temp}{whether to unzip in a temporary directory}

\item{outdir}{character: one of \code{path} or \code{tempdir}}

\item{call}{character: system unzip invocation}
}
\value{
invisible path to unzipped file
}
\description{
Unzips a NONMEM file, by default the xml output
of a NONMEM run.  Uses 7z decompression. If an unzipped
file with the corresponding name already exists, it is removed
before unzipping.
}
\examples{
options(nmDir = getOption('qpExampleDir'))
dir <- getOption('qpExampleDir')
unz <- nm_unzip('example1')
unz
unlink(unz)
run <- 'example1'
file <- 'example1.xml'
zipped <- 'example1.xml.7z'
path <- file.path(dir, run, file)
zpath <-file.path(dir, run, zipped)
file.exists(path)
file.exists(zpath)
to <- nm_unzip(zipped = path)
file.exists(to)
unlink(to)
to <- nm_unzip(zipped = zpath)
file.exists(to)
unlink(to)
}
\seealso{
Other nm: 
\code{\link{definitions.nonmem}()},
\code{\link{extract_varcov}()},
\code{\link{get_est_table}()},
\code{\link{get_est_table.default}()},
\code{\link{get_est_table.nonmem}()},
\code{\link{get_ofv}()},
\code{\link{get_ofv.character}()},
\code{\link{nm_cov_table}()},
\code{\link{read_nm_qp}()},
\code{\link{read_nm_qp.character}()},
\code{\link{read_nm_qp.nonmem}()},
\code{\link{sample_uncert_qp}()}
}
\concept{nm}
\keyword{internal}
